{
  "name": "@pathway/workspace",
  "private": true,
  "scripts": {
    "build": "pnpm -r --sort run build",
    "test": "pnpm -r --sort run test"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.3.4",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
